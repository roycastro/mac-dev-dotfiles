local colors = require("colors")

-- Equivalent to the --bar domain
sbar.bar({
  topmost = "window",
  height = 80,
  color = colors.black_manta,
  padding_right = 2,
  padding_left = 2,
  position = "left"
})
